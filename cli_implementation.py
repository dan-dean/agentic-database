from async_agentic_database import AsyncAgenticDatabase

async_db = AsyncAgenticDatabase()

while True:
    input = input("Enter a command: ")
    if input == "help":
        print("Commands: help, set_default_database, list_databases, add_document, queue_size, status, send_query, start_thread, exit")
    elif 
    
